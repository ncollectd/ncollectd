when:
  - event: cron

steps:
  build-fedora-43-clang-analyzer:
    image: ncollectd/ci-fedora:43
    commands:
      - scan-build cmake -B build-fedora-43-clang-analyzer -S .
      - scan-build cmake --build build-fedora-43-clang-analyzer
