when:
  - event: pull_request
  - event: push

steps:
  build-fedora-43-clang:
    image: ncollectd/ci-fedora:43
    commands:
      - CC=clang cmake -B build-fedora-43-clang -S .
      - cmake --build build-fedora-43-clang
